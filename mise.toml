[tools]
go = "1.25.5"
golangci-lint = "2.12.2"
goreleaser = "2.16.0"

[tasks.build]
description = "Build the go-bin-template binary"
run = "go build -o go-bin-template ."

[tasks.lint]
description = "Run golangci-lint"
run = "golangci-lint run ./..."

[tasks.test]
description = "Run tests with coverage"
run = "go test -v -count=1 -cover -coverprofile=coverage.out -covermode=atomic ./..."

[tasks.release]
description = "Run GoReleaser (snapshot)"
run = "goreleaser release --snapshot --clean"

[tasks.clean]
description = "Remove build artifacts"
run = "rm -rf go-bin-template dist coverage.out"
